@extends('adminlte::page')
@section('title', 'Complete Hisab')
@section('content_header')<h1>Complete Hisab</h1>@stop
@section('content')
<form class="card card-body mb-3" method="GET"><div class="form-row"><div class="col-md-3"><input type="date" name="from" class="form-control" value="{{ request('from', $from) }}"></div><div class="col-md-3"><input type="date" name="to" class="form-control" value="{{ request('to', $to) }}"></div><div class="col-md-3"><select name="machine_id" class="form-control"><option value="">All Machines</option>@foreach($machines as $machine)<option value="{{ $machine->id }}" @selected(request('machine_id') == $machine->id)>{{ $machine->name }}</option>@endforeach</select></div><div class="col-md-3"><button class="btn btn-primary btn-block">Filter</button></div></div></form>
<div class="mb-2"><a class="btn btn-sm btn-success" href="{{ route('reports.export', ['report' => 'complete-hisab', 'format' => 'excel'] + request()->query()) }}">Excel</a> <a class="btn btn-sm btn-danger" href="{{ route('reports.export', ['report' => 'complete-hisab', 'format' => 'pdf'] + request()->query()) }}">PDF</a></div>
<div class="card"><div class="card-body table-responsive p-0"><table class="table table-striped"><thead><tr><th>Machine</th><th>Total Hours</th><th>Gross</th><th>Total Diesel</th><th>Diesel Cost</th><th>Payments</th><th>Expenses</th><th>Net Balance</th></tr></thead><tbody>@foreach($rows as $row)<tr><td>{{ $row['machine'] }}</td><td>{{ number_format($row['total_hours'], 2) }}</td><td>{{ number_format($row['gross_amount'], 2) }}</td><td>{{ number_format($row['total_diesel_liters'], 2) }}</td><td>{{ number_format($row['diesel_cost'], 2) }}</td><td>{{ number_format($row['total_payments'], 2) }}</td><td>{{ number_format($row['total_expenses'], 2) }}</td><td>{{ number_format($row['net_balance'], 2) }}</td></tr>@endforeach</tbody></table></div></div>
@stop
